{
  "name": "sdms-performance-test",
  "version": "1.0.0",
  "description": "SDMS 성능 분석 도구",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report reports/html",
    "monitor": "node db-monitor/db-alert-monitor.js",
    "monitor:profile": "node db-monitor/mssql-profiler.js",
    "monitor:test-webhook": "node -e \"const WebhookSender = require('./db-monitor/utils/webhook-sender'); const config = require('./db-monitor/config/alert-config.json'); const sender = new WebhookSender(config.webhook); sender.sendTestMessage().then(() => process.exit());\""
  },
  "keywords": [
    "performance",
    "testing",
    "playwright"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mssql": "^12.2.0",
    "nodemailer": "^6.9.0",
    "xml2js": "^0.6.0"
  }
}
